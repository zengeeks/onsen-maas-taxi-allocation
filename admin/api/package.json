{
  "name": "onsen-maas-admin-api",
  "description": "Onsen Maas reference admin's api by Azure Functions",
  "version": "1.2.0",
  "engines": {
    "node": ">=16",
    "npm": ">=7"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "lint": "prettier --check . && eslint --ext \".ts,.js\" --ignore-path .gitignore .",
    "lint:fix": "prettier --write . && eslint --ext \".ts,.js\" --ignore-path .gitignore .",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/cosmos": "^3.15.1",
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "^17.0.35",
    "@typescript-eslint/eslint-plugin": "^5.26.0",
    "@typescript-eslint/parser": "^5.26.0",
    "eslint": "^8.16.0",
    "typescript": "^4.0.0"
  }
}
